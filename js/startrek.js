/**
 * @file
 * Attaches behaviors for the startrek module.
 */

(function ($, Drupal, document) {

  'use strict';

  /**
   * Attaches the startrek behavior.
   *
   * @type {Drupal~behavior}
   *
   * @prop {Drupal~behaviorAttach} attach
   *   Attach startrek functionality on the content pages.
   */
  Drupal.behaviors.startrekBehavior = {

    attach: function (context) {
      // @todo
    }

  }
})(jQuery, Drupal, document);
